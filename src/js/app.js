import '../sass/main.scss';

let content = `In order to achieve full production and developemnt potential it's recommended to use this scaffold
with the Visual studio code editor and live server in older to achieve live reloading`;

$(document).ready(function() {
  $("#jquery-text").text(content);
});